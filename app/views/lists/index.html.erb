<div class="container-fluid">
  <div class="d-flex justify-content-center">
    <h1>All lists</h1>
  </div>
  <div class="d-flex justify-content-center">
    <div><%= link_to "Create a new List", new_list_path, class: "btn btn-blue" %></div>
  </div>
</div>
<div class="d-flex justify-content-center">
  <% @lists.each do |list| %>
    <div class="card" style="width: 18rem;">
      <img class="card-img-top" src="http://source.unsplash.com/featured/?<%= list.name %>+movie&<%= rand(1000) %>)" alt="Card image cap">
      <div class="card-body">
        <p class="card-text"> <%=link_to list.name, list_path(list) %></p>
      </div>
    </div>
  <% end %>
</div>
<%= cl_image_tag("ziugdsyakeppxpgtfojs",
      width: 400, height: 300, crop: :fill) %>
